# Required metadata
sonar.projectKey=ReactJS-App
sonar.projectName=ReactJS-App
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=.

# Encoding of the source files
sonar.sourceEncoding=UTF-8
